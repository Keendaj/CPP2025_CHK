#pragma once
#include <string>

class Subject1{

    public:
        void doSomething(double a, const char* b);
        int calculate(int x, int y, int z);
        void printMessage(const std::string& msg);
        double divide(double a, double b);
};